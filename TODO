
Permanent TODO priorities:
- more and better examples
- documentation, commented code
====================================

v. 0.2
====================================

Perl debugger interaction:
- source viewer output as a sequence of pages when lines > 50
- display value on mouse over variable (using jQuery and request to debugger?)
- function names to be links and to step into on click
- show source code and highlight current line only when current line is known, otherwise do not display source code
- show highlighted HTML source code when scripts produce HTML within the debugger GUI

Locally executed scripts:
- option to discard user PATH from script PATH
- HTML formatted STDERR output

GUI:
- working maximized window setting on Mac

Configuration & help:
- 'config' URL with a nice HTML interface
- 'restart' URL for applying configuration changes that need restart
- 'help' URL; keyboard shortcuts listed

Compilation, packaging & installation:
- build script for command line compilation of the browser
- windeployqt script for win32 deployment
