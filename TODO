
Permanent TO-DO:
- up-to-date README,
- commented code,
- up-to-date translations,
- GitHub Wiki pages,
- GitHub webpage.

v. 0.2
====================================
- browser settings file can be in:
    1.) the browser binary folder,
    2.) the current user settings folder,
    3.) the default system settings folder
- support for separate package settings INI files
    package settings can be:
    1.) inside the browser settings file,
    2.) supplied from command line option
- {user_settings_folder} alias inside settings files
    {user_settings_folder} applicable for:
        browser/perl
        browser/perllib
        browser/logging_directory
        package/root
        package/data_root
- built-in default values for browser and package settings

- better documentation for censor.pl
- security tests in separate files
- separate resource file for censor.pl

- start Perl debugger in script environment if script is in the current package
    and if script is outside the current package
    ask for command line arguments and environment variables
- dbgformatter.pl fatpacked together with Syntax::Highlight::Engine::Kate

- single template for all HTML error messages
- translations for all HTML error messages
- HTML error pages for missing browser settings file and missing start page
- user-supplied JS prompt when check-user-input-before-quit finds user input

- better SQLite test

v. 0.3
====================================
- internal JavaScript-based code editor - ACE;
    code editor will edit settings files too
