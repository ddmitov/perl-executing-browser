
# Settings file for Perl Executing Browser

perl=perl/bin/perl
# Enter relative path to your Perl interpreter.
# It is resolved using the directory of this settings file.

start_fullscreen=
# Optional setting, disabled by default.
# Type 'enable' to start browser in fullscreen mode.
