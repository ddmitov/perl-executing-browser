#!/usr/bin/perl

use strict;
use warnings;

my $value = 1;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
